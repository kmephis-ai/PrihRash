+test('stale STAGING retirement is explicit, exact-state guarded, and defaults closed', () => {
+  assert.match(runtime, /PRIHRASH_INITIAL_BOOTSTRAP_STAGING_RETIREMENT_AUTHORIZED/);
+  assert.match(runtime, /=== 'true'/);
+  assert.match(runtime, /options\.allowStaleStagingRetirement === true/);
+  assert.match(runtime, /recoverySurface\.reason !== 'STAGING_RUN_PRESENT'/);
+  assert.match(runtime, /retireInitialBootstrapStaleStagingRun\([\s\S]*?observation\.snapshotDigest,[\s\S]*?dependencies\.clock\.now\(\)/);
+  assert.match(runtime, /staleStagingRetired = true/);
+  assert.match(runtime, /!staleStagingRetired && recoverySurface\.reason === 'RESIDUAL_REFERENCE_STATE_WITHOUT_RUN'/);
+});
+
+test('retirement authorization propagates only through the guarded R1 workflow chain', () => {
+  assert.match(autocontinue, /Recovery-State: STAGING_RETIRABLE/);
+  assert.match(autocontinue, /allow_staging_retirement='true'/);
+  assert.match(autocontinue, /allow_staging_retirement:\$retirement/);
+  assert.match(orchestrator, /allow_staging_retirement:/);
+  assert.match(orchestrator, /STAGING_AUTHORITY_AMBIGUOUS/);
+  assert.match(orchestrator, /R1_BOOTSTRAP_ALLOW_STAGING_RETIREMENT/);
+  assert.match(child, /R1_BOOTSTRAP_ALLOW_STAGING_RETIREMENT/);
+  assert.match(child, /allow_staging_retirement: allowStagingRetirement/);
+  assert.match(bootstrap, /allow_staging_retirement:/);
+  assert.match(bootstrap, /PRIHRASH_INITIAL_BOOTSTRAP_STAGING_RETIREMENT_AUTHORIZED=\$\{\{ inputs\.allow_staging_retirement \}\}/);
+});
diff --git a/tests/tooling/r1-initial-bootstrap-autocontinue-workflow.test.mjs b/tests/tooling/r1-initial-bootstrap-autocontinue-workflow.test.mjs
index 87beea5..5cb7fec 100644
--- a/tests/tooling/r1-initial-bootstrap-autocontinue-workflow.test.mjs
+++ b/tests/tooling/r1-initial-bootstrap-autocontinue-workflow.test.mjs
@@ -45,6 +45,7 @@ test('R1 autocontinue requires explicit root-cause attempt evidence or one prove
   assert.match(workflow, /pulls\/\$source_pr_number\/files\?per_page=100/);
   assert.match(workflow, /ROOT_CAUSE_EVIDENCE_MISSING/);
   assert.match(workflow, /STAGING_RESUMABLE/);
+  assert.match(workflow, /STAGING_RETIRABLE/);
   assert.match(workflow, /NOT_SINGLE_MERGED_MAIN_PR/);
   assert.match(workflow, /parents \| length\) == 1/);
   assert.match(workflow, /r1-initial-bootstrap-orchestrator-evidence-\$interrupted_run_id/);
@@ -71,6 +72,7 @@ test('R1 autocontinue requires explicit root-cause attempt evidence or one prove
   assert.match(workflow, /R1_BOOTSTRAP_AUTOCONTINUE_ALREADY_DISPATCHED/);
   assert.match(workflow, /r1-initial-bootstrap-orchestrator\.yml\/dispatches/);
   assert.match(workflow, /allow_staging_resume/);
+  assert.match(workflow, /allow_staging_retirement/);
   assert.match(workflow, /--data "\$dispatch_payload"/);
   assert.doesNotMatch(workflow, /r1-yandex-readiness\.yml\/dispatches/);
   assert.doesNotMatch(workflow, /r1-initial-shadow-bootstrap\.yml\/dispatches/);
diff --git a/tests/tooling/r1-initial-bootstrap-orchestrator-workflow.test.mjs b/tests/tooling/r1-initial-bootstrap-orchestrator-workflow.test.mjs
index a873f71..d31e67d 100644
--- a/tests/tooling/r1-initial-bootstrap-orchestrator-workflow.test.mjs
+++ b/tests/tooling/r1-initial-bootstrap-orchestrator-workflow.test.mjs
@@ -16,6 +16,7 @@ test('R1 bootstrap orchestrator has one manual entrypoint and no autonomous trig
 
   assert.match(workflow, /workflow_dispatch:/);
   assert.match(workflow, /allow_staging_resume:/);
+  assert.match(workflow, /allow_staging_retirement:/);
   assert.match(workflow, /default: 'false'/);
   assert.doesNotMatch(workflow, /\n\s+(push|pull_request|schedule|repository_dispatch|workflow_run):/);
   assert.match(workflow, /github\.repository == 'kmephis-ai\/PrihRash'/);
@@ -54,6 +55,8 @@ test('orchestrator proceeds only from bounded recovery states and performs at mo
   assert.match(workflow, /STAGING_RUN_PRESENT/);
   assert.match(workflow, /inputs\.allow_staging_resume/);
   assert.match(workflow, /R1_BOOTSTRAP_ORCHESTRATOR_STAGING_RESUME_ARMED/);
+  assert.match(workflow, /R1_BOOTSTRAP_ORCHESTRATOR_STAGING_RETIREMENT_ARMED/);
+  assert.match(workflow, /R1_BOOTSTRAP_ORCHESTRATOR_STAGING_AUTHORITY_AMBIGUOUS/);
   assert.match(workflow, /R1_BOOTSTRAP_ORCHESTRATOR_INITIAL_RECOVERY_BLOCKED/);
   assert.match(workflow, /r1-bootstrap-orchestrator-child-workflow\.mjs readiness/);
   assert.match(workflow, /\.code == "READINESS_READY"/);
@@ -85,7 +88,9 @@ test('child dispatcher can launch only canonical child workflows and fails close
   assert.match(dispatcher, /branch\?\.protected !== true/);
   assert.match(dispatcher, /activeMainDispatch/);
   assert.match(dispatcher, /event === 'workflow_dispatch'/);
-  assert.match(dispatcher, /body: JSON\.stringify\(\{ ref: 'main' \}\)/);
+  assert.match(dispatcher, /allow_staging_retirement/);
+  assert.match(dispatcher, /R1_BOOTSTRAP_ALLOW_STAGING_RETIREMENT/);
+  assert.match(dispatcher, /body: JSON\.stringify/);
   assert.match(dispatcher, /CHILD_WORKFLOW_ALREADY_ACTIVE/);
   assert.match(dispatcher, /CHILD_WORKFLOW_AMBIGUOUS/);
   assert.match(dispatcher, /Invoke exact initial bootstrap tag once/);
@@ -94,7 +99,7 @@ test('child dispatcher can launch only canonical child workflows and fails close
   assert.match(dispatcher, /result\.invokeStepConclusion = 'UNKNOWN'/);
 });
 
-test('orchestrator publishes one retained privacy-safe evidence artifact including resume authorization', async () => {
+test('orchestrator publishes one retained privacy-safe evidence artifact including staging authorizations', async () => {
   const workflow = await text(WORKFLOW);
 
   assert.match(workflow, /r1-initial-bootstrap-orchestrator-evidence-\$\{\{ github\.run_id \}\}/);
@@ -102,6 +107,7 @@ test('orchestrator publishes one retained privacy-safe evidence artifact includi
   assert.match(workflow, /R1_BOOTSTRAP_ORCHESTRATOR_COMMITTED/);
   assert.match(workflow, /R1_BOOTSTRAP_ORCHESTRATOR_POST_INVOKE_RECOVERY_CLASSIFIED/);
   assert.match(workflow, /stagingResumeAuthorized/);
+  assert.match(workflow, /stagingRetirementAuthorized/);
   assert.match(workflow, /retention-days: 30/);
   assert.doesNotMatch(workflow, /rawPayload|row_count|amount|notes/i);
 });
diff --git a/tests/tooling/r1-initial-shadow-bootstrap-workflow.test.mjs b/tests/tooling/r1-initial-shadow-bootstrap-workflow.test.mjs
index 5f6a969..6c0b0d5 100644
--- a/tests/tooling/r1-initial-shadow-bootstrap-workflow.test.mjs
+++ b/tests/tooling/r1-initial-shadow-bootstrap-workflow.test.mjs
@@ -18,6 +18,7 @@ test('initial shadow bootstrap workflow is manual main-only and structurally gat
   const workflow = await text(WORKFLOW);
 
   assert.match(workflow, /workflow_dispatch:/);
+  assert.match(workflow, /allow_staging_retirement:/);
   assert.doesNotMatch(workflow, /\n\s+(push|pull_request|schedule|repository_dispatch):/);
   assert.match(workflow, /github\.ref == 'refs\/heads\/main'/);
   assert.match(workflow, /github\.repository == 'kmephis-ai\/PrihRash'/);
@@ -57,6 +58,7 @@ test('workflow deploys a private trigger-free bootstrap-only version with exact
   assert.match(workflow, /--memory 256m/);
   assert.match(workflow, /--execution-timeout 150s/);
   assert.match(workflow, /--no-logging/);
+  assert.match(workflow, /--environment \"PRIHRASH_INITIAL_BOOTSTRAP_STAGING_RETIREMENT_AUTHORIZED=\$\{\{ inputs\.allow_staging_retirement \}\}\"/);
   assert.match(workflow, /serverless trigger list/);
   assert.doesNotMatch(workflow, /serverless trigger create/);
   for (const key of [
diff --git a/tests/tooling/r1-status.test.mjs b/tests/tooling/r1-status.test.mjs
index b30a32a..d8882ab 100644
--- a/tests/tooling/r1-status.test.mjs
+++ b/tests/tooling/r1-status.test.mjs
@@ -28,6 +28,7 @@ function orchestrator(overrides = {}) {
     postRecoveryVerdict: 'RECOVERY_REQUIRED',
     postRecoveryReason: 'RESIDUAL_REFERENCE_STATE_MATCHES_AUTHORITATIVE',
     stagingResumeAuthorized: false,
+    stagingRetirementAuthorized: false,
     ...overrides,
   };
 }
