yReadback = parseInitialBootstrapIdentityManifestRows(candidate.run.id, identityRows);
     } catch (error) {
-      if (error instanceof InitialBootstrapMetadataExecutorError) throw error;
-      throw new InitialBootstrapMetadataExecutorError('IDENTITY_MANIFEST_READBACK_MISMATCH');
+      if (error instanceof InitialBootstrapIdentityManifestError) throw error;
+      throw new InitialBootstrapMetadataExecutorError('IDENTITY_MANIFEST_READ_FAILED');
+    }
+    if (
+      identityReadback.runState !== 'STAGING'
+      || identityReadback.runSnapshotDigest !== candidate.run.sourceSnapshotDigest
+      || identityReadback.snapshotDigest !== candidate.snapshot.snapshotDigest
+      || identityReadback.snapshotRowCount !== candidate.snapshot.rowCount
+    ) {
+      throw new InitialBootstrapMetadataExecutorError('IDENTITY_MANIFEST_CONTEXT_READBACK_MISMATCH');
+    }
+    if (!initialBootstrapIdentityManifestsEqual(identityReadback.manifest, identityManifestWrite.manifest)) {
+      throw new InitialBootstrapMetadataExecutorError('IDENTITY_MANIFEST_CONTENT_READBACK_MISMATCH');
     }
 
     let postAdmissionRows;
diff --git a/tests/integration/yandex-initial-bootstrap-invoker.test.mjs b/tests/integration/yandex-initial-bootstrap-invoker.test.mjs
index 6e0eeb5..732c6a4 100644
--- a/tests/integration/yandex-initial-bootstrap-invoker.test.mjs
+++ b/tests/integration/yandex-initial-bootstrap-invoker.test.mjs
@@ -113,6 +113,20 @@ test('safe non-success Function results remain exact bounded output and exit non
       applicationPhase: 'FRESH_CLAIM_WRITE',
       metadataFailureCode: 'METADATA_EXECUTOR_RUN_READBACK_MISMATCH',
     },
+    {
+      status: 'FAIL',
+      code: 'INITIAL_BOOTSTRAP_RUNTIME_FAILED',
+      runtimeCode: 'REFERENCE_APPLICATION_METADATA_FAILED',
+      applicationPhase: 'FRESH_CLAIM_WRITE',
+      metadataFailureCode: 'METADATA_EXECUTOR_IDENTITY_MANIFEST_CONTENT_READBACK_MISMATCH',
+    },
+    {
+      status: 'FAIL',
+      code: 'INITIAL_BOOTSTRAP_RUNTIME_FAILED',
+      runtimeCode: 'REFERENCE_APPLICATION_METADATA_FAILED',
+      applicationPhase: 'FRESH_CLAIM_WRITE',
+      metadataFailureCode: 'IDENTITY_MANIFEST_MALFORMED_MANIFEST_ROW',
+    },
   ];
 
   for (const value of values) {
diff --git a/tests/migration-simulation/initial-bootstrap-metadata-executor.test.mjs b/tests/migration-simulation/initial-bootstrap-metadata-executor.test