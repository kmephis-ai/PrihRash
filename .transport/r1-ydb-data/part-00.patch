diff --git a/.github/workflows/r1-initial-shadow-bootstrap.yml b/.github/workflows/r1-initial-shadow-bootstrap.yml
index 4812c79..c6dd32a 100644
--- a/.github/workflows/r1-initial-shadow-bootstrap.yml
+++ b/.github/workflows/r1-initial-shadow-bootstrap.yml
@@ -566,6 +566,23 @@ jobs:
                   else null
                   end
                 ),
+                ydbDataFailureCode: (
+                  if $code == "INITIAL_BOOTSTRAP_RUNTIME_FAILED"
+                     and (.ydbDataFailureCode | type) == "string"
+                     and (.ydbDataFailureCode | IN(
+                       "YDB_TRANSPORT_SDK_SHAPE_INVALID",
+                       "YDB_TRANSPORT_PARAMETER_VALUE_INVALID",
+                       "YDB_TRANSPORT_PARAMETER_TYPE_UNSUPPORTED",
+                       "YDB_TRANSPORT_TIMESTAMP_PRECISION_UNSUPPORTED",
+                       "YDB_TRANSPORT_QUERY_EXECUTION_FAILED",
+                       "YDB_TRANSPORT_CLIENT_CONFIG_INVALID",
+                       "YDB_ADAPTER_WRITE_REQUIRES_TRANSACTION",
+                       "YDB_COMMIT_OUTCOME_UNKNOWN"
+                     ))
+                  then .ydbDataFailureCode
+                  else null
+                  end
+                ),
                 httpStatus: (
                   if $code == "INITIAL_BOOTSTRAP_INVOKE_HTTP_FAILED"
                   then .httpStatus
diff --git a/scripts/invoke-yandex-initial-bootstrap.mjs b/scripts/invoke-yandex-initial-bootstrap.mjs
index f56417a..6760826 100644
--- a/scripts/invoke-yandex-initial-bootstrap.mjs
+++ b/scripts/invoke-yandex-initial-bootstrap.mjs
@@ -165,6 +165,17 @@ const METADATA_FAILURE_CODES = new Set([
   'YDB_PARAMETER_INVALID_JSON_DOCUMENT',
 ]);
 
+const YDB_DATA_FAILURE_CODES = new Set([
+  'YDB_TRANSPORT_SDK_SHAPE_INVALID',
+  'YDB_TRANSPORT_PARAMETER_VALUE_INVALID',
+  'YDB_TRANSPORT_PARAMETER_TYPE_UNSUPPORTED',
+  'YDB_TRANSPORT_TIMESTAMP_PRECISION_UNSUPPORTED',
+  'YDB_TRANSPORT_QUERY_EXECUTION_FAILED',
+  'YDB_TRANSPORT_CLIENT_CONFIG_INVALID',
+  'YDB_ADAPTER_WRITE_REQUIRES_TRANSACTION',
+  'YDB_COMMIT_OUTCOME_UNKNOWN',
+]);
+
 function nonBlank(value) {
   return typeof value === 'string' && value.length > 0 && value === value.trim();
 }
@@ -270,13 +281,15 @@ function parseExactFunctionResult(stdout) {
   if (
     result.status === 'FAIL'
     && result.code === 'INITIAL_BOOTSTRAP_RUNTIME_FAILED'
-    && exactKeys(result, ['status', 'code', 'runtimeCode', 'applicationPhase', 'metadataFailureCode'])
+    && exactKeys(result, ['status', 'code', 'runtimeCode', 'applicationPhase', 'metadataFailureCode', 'ydbDataFailureCode'])
     && typeof result.runtimeCode === 'string'
     && REFERENCE_AWARE_RUNTIME_CODES.has(result.runtimeCode)
     && (result.applicationPhase === null
       || (typeof result.applicationPhase === 'string' && APPLICATION_PHASES.has(result.applicationPhase)))
     && (result.metadataFailureCode === null
       || (typeof result.metadataFailureCode === 'string' && METADATA_FAILURE_CODES.has(result.metadataFailureCode)))
+    && (result.ydbDataFailureCode === null
+      || (typeof result.ydbDataFailureCode === 'string' && YDB_DATA_FAILURE_CODES.has(result.ydbDataFailureCode)))
   ) {
     return Object.freeze({
       status: 'FAIL',
@@ -284,6 +297,7 @@ function parseExactFunctionResult(stdout) {
       runtimeCode: result.runtimeCode,
       applicationPhase: result.applicationPhase,
       metadataFailureCode: result.metadataFailureCode,
+      ydbDataFailureCode: result.ydbDataFailureCode,
     });
   }
   if (
diff --git a/scripts/package-yandex-initial-bootstrap-function.mjs b/scripts/package-yandex-initial-bootstrap-function.mjs
index 296837d..201a024 100644
--- a/scripts/package-yandex-initial-bootstrap-function.mjs
+++ b/scripts/package-yandex-initial-bootstrap-function.mjs
@@ -36,6 +36,7 @@ const INDEX_SOURCE = `const MODULE_LOAD_FAILURE = Object.freeze({
   runtimeCode: 'REFERENCE_FUNCTION_MODULE_LOAD_FAILED',
   applicationPhase: null,
   metadataFailureCode: null,
+  ydbDataFailureCode: null,
 });
 const HANDLER_UNCAUGHT_FAILURE = Object.freeze({
   status: 'FAIL',
@@ -43,6 +44,7 @@ const HANDLER_UNCAUGHT_FAILURE = Object.freeze({
   runtimeCode: 'REFERENCE_FUNCTION_HANDLER_UNCAUGHT',
   applicationPhase: null,
   metadataFailureCode: null,
+  ydbDataFailureCode: null,
 });
 
 export async function initialBootstrapHandler(event, context) {
diff --git a/scripts/verify-yandex-initial-bootstrap-package.mjs b/scripts/verify-yandex-initial-bootstrap-package.mjs
index d3d1447..912026d 100644
--- a/scripts/verify-yandex-initial-bootstrap-package.mjs
+++ b/scripts/verify-yandex-initial-bootstrap-package.mjs
@@ -13,6 +13,7 @@ const EXPECTED_INDEX_SOURCE = `const MODULE_LOAD_FAILURE = Object.freeze({
   runtimeCode: 'REFERENCE_FUNCTION_MODULE_LOAD_FAILED',
   applicationPhase: null,
   metadataFailureCode: null,
+  ydbDataFailureCode: null,
 });
 const HANDLER_UNCAUGHT_FAILURE = Object.freeze({
   status: 'FAIL',
@@ -20,6 +21,7 @@ const HANDLER_UNCAUGHT_FAILURE = Object.freeze({
   runtimeCode: 'REFERENCE_FUNCTION_HANDLER_UNCAUGHT',
   applicationPhase: null,
   metadataFailureCode: null,
+  ydbDataFailureCode: null,
 });
 
 export async function initialBootstrapHandler(event, context) {
diff --git a/src/runtime/initialBootstrapReferenceAwareJob.ts b/src/runtime/initialBootstrapReferenceAwareJob.ts
index 1069fa7..431b604 100644
--- a/src/runtime/initialBootstrapReferenceAwareJob.ts
+++ b/src/runtime/initialBootstrapReferenceAwareJob.ts
@@ -20,6 +20,7 @@ import {